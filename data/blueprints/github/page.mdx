---
title: GitHub
description: Work GitHub from Ren — issues, pull requests, reviews, and repositories
longDescription: Run the GitHub side of your work without leaving the project.
  Triage and label issues, open branches and pull requests under Conventional
  Commits, watch CI, review diffs and leave inline comments, approve or request
  changes, and merge when it is green. Clone, fork and configure repositories,
  manage remotes, and cut releases. Authentication covers HTTPS tokens, SSH keys
  and `gh` CLI login, so an agent can get itself on the host before it does
  anything else.
---
