---
title: Remotion
favicon: https://www.remotion.dev/img/logo.png
description: Make videos programmatically in React — compositions, animation, and rendering
longDescription: Write videos as React code with Remotion. Scaffold a project
  and its compositions, animate with frame-driven markup, layer in captions,
  audio, maps, and interactive elements, preview the result in Remotion Studio,
  then render to a file. Remotion maintains these skills in its own repository,
  so the guidance tracks the framework rather than a snapshot of it.
---
